name: Map DXF tools (synthetic only)
on:
  push:
    paths:
      - "tools/dxf_to_map.py"
      - "tests/test_dxf_to_map.py"
      - "tests/test_dxf_private_inspect.py"
      - "tools/inspect_dxf_private.py"
      - "requirements-map.txt"
      - ".github/workflows/map-tools.yml"
  pull_request:
    paths:
      - "tools/dxf_to_map.py"
      - "tools/inspect_dxf_private.py"
      - "tests/test_dxf_to_map.py"
      - "tests/test_dxf_private_inspect.py"
      - "requirements-map.txt"
      - ".github/workflows/map-tools.yml"
  workflow_dispatch:
jobs:
  test-map-tools:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install -r requirements-map.txt
      - run: python -m unittest discover -s tests -p "test_*.py" -v
