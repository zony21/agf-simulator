# AGF搬送シミュレーター 全体実装計画・進捗管理

> 更新基準日: 2026-09-25 / 対象: オフライン分析用の公開安全なコードと、別管理の非公開CAD設定。  
> **最新の完了チェックは [Issue #2](https://github.com/zony21/agf-simulator/issues/2) が正本**。この文書は実装順序・作業分解・受入条件を定義する。コード実装と実図面の承認を混同しない。  
> 実装PR: [#1](https://github.com/zony21/agf-simulator/pull/1)（`feat/event-simulator-ui`、2026-09-25時点では未マージ）。

## 0. 完了の定義と進捗表示

| 記号 | 判定 |
| --- | --- |
| ✅ 実装済み（PR） | 対象コードと合成テストがPRにあり、CI成功を確認。mainへのマージ・実図面受入は別。 |
| 🔄 一部実装 | 一部機能・下書き・ツールはあるが、工程の受入条件は未達。 |
| ☐ 未完了 | 必要機能または受入・確認が未完了。 |
| 🔒 非公開確認待ち | 現場由来の位置、設備動作、経路などの個別レビューが必要。公開Git上で情報を捏造・公開しない。 |

**工程のチェックを完了にする条件：** 実装・自動テスト・画面／シナリオ検証・仕様上の未確定点の扱いが揃い、関連PR／CI／レビュー根拠をIssueに記載する。下書き線の作成・仮定値の入力だけでは物理走行の完了としない。

## 1. 工程サマリー（2026-09-25）

| 工程 | 現状 | 到達点／残作業 | 依存 |
| --- | --- | --- | --- |
| M0 基盤・合成シナリオ | ✅ PR実装済み | 離散イベント、搬送01～05の基本流れ、AGF選定・充電、設定、再生・比較・CSV。実条件への適合は未認定。 | — |
| M1 CAD取込と直感編集 | 🔄 一部実装 | 非公開SVG／PNGと未割当参考ガイド4本、ドラッグ編集・JSON保存まで実装。縮尺・原点・図上レビューと設備／荷役点の対応付けが未完了。 | M0 |
| M2 承認済み走行グラフ | 🔄 型・レビューUI実装済み | 区間ごとの別台帳と未確認除外の位相グラフを実装。実図上での通行確定、共有接続・停止点、物理走行化は未完了。 | M1 |
| M3 AGF物理走行 | ☐ 未完了 | 承認済みグラフ上の経路探索、空走・積載・荷役、時刻別位置と区間所要時間。 | M2 |
| M4 交通・干渉 | ☐ 未完了 | 区間予約、交差・すれ違い、シャッター、待ち／譲り合い。 | M2–M3 |
| M5 搬送・設備詳細 | 🔄 基本イベントのみ | 01～05と包装・整列機・マガジン・倉庫の占有／許可／保留／回復を物理移動と統合。 | M3–M4 |
| M6 電池・充電連動 | 🔄 基本モデルのみ | 距離・動作ベース消費、充電場所への走行、2口占有・充電待ち・復帰。 | M3–M4 |
| M7 2D再生・分析 | 🔄 基本UIのみ | 走行中位置、待ち・稼働率・空走・ボトルネック、同一入力での方式比較、根拠付きCSV。 | M3–M6 |
| M8 統合受入・リリース | ☐ 未完了 | 3時間再生、正常／異常／境界、実図レビュー、操作確認、公開範囲点検、PRレビュー・マージ。 | M0–M7 |

工程M5～M7は既存の合成モデルを**拡張**する。イベントエンジンを不必要に作り直さない。実装順は依存関係に従い、実図面確認と公開可能な合成テスト作成は並行してよい。

## 2. 工程別作業・受入チェック

### M0 基盤・合成シナリオ — ✅ PR実装済み
- [x] イベントを整数ミリ秒と安定した処理順で実行し、履歴／スナップショットを保存。
- [x] 01→包装・ラベル・搬出OK→02、03補充、明示許可つき04／05の合成シナリオ。
- [x] 4台AGF・2口充電、目的地エリア優先と低残量優先、ロケーション／同列発行制約。
- [x] 系列別分間隔、シナリオ設定、3時間再生、同一入力での方式比較、イベントCSV。
- [x] 合成テストとCI（[Harness checks](https://github.com/zony21/agf-simulator/actions/runs/36030083070)、[Map DXF tools](https://github.com/zony21/agf-simulator/actions/runs/36030083047)）。  
**受入範囲:** 概念／合成モデル。実走行ETAや現場スループットの保証ではない。

### M1 CAD取込・図面編集 — 🔄
- [x] 非公開CADの表示専用プレビュー生成（単位未設定DXFには明示的なmm仮定）。
- [x] AGF図形の除外指定、端末内読込、SVG画像ハッシュ／viewBoxへの紐付け。
- [x] 初期参考ガイドの非公開生成・読込、ドラッグ／点挿入・削除／Undo・Redo／拡大／ローカルJSON保存。
- [ ] 🔒 参考ガイド4本の位置・通行可否を図上レビューし、必要な区間を描き直す。
- [ ] 🔒 設備・荷受け／荷下ろし・旋回・停止・シャッターの点を図面上で確認・登録する。
- [ ] 🔒 既知寸法との縮尺照合、基準原点、未展開INSERTとレイヤー欠落をレビューする。  
**受入:** 対象位置・線の確認状態と未確定箇所を一覧化でき、下書きと承認対象が混同されない。

### M2 承認済み走行グラフ — 🔄
- [x] 非公開CADに紐づく区間レビュー台帳を別管理し、通行可否・方向・車線数・同時通過・ゲートを独立入力できるUI。
- [x] 未確認／通行不可／未割当ガイドを除外する位相グラフ契約と、点・区間・区分変更後の旧レビュー失効を合成テスト。
- [ ] 確認済みの通行区間だけをノード／エッジに対応付ける。共有接続点を明示。
- [ ] 通行可否・方向・車線・同時通過・ゲート／シャッター条件を独立した属性にする。
- [ ] 荷役点、曲がる点、停止／待機点、充電位置、倉庫ロケーションを区別する。
- [ ] 未承認区間・逆走・ゲート不整合・接続切れ・不正な入口を拒否する合成テスト。
- [ ] 🔒 個別の01～05／充電ルートについて、空走と積載走行の両方の経路を確認する。  
**受入:** `draft-only` を経路探索に流さず、根拠を確認した区間だけが通行対象となる。現時点の[区間レビュー・位相グラフ契約](reviewed-topology-contract.md)は距離／ETA／交通制御を提供せず、実図面の位置・通行の承認までは工程未完了。

### M3 AGF物理走行 — ☐
- [ ] AGF現在位置から荷受け点への空走経路を探索し、積載経路を分ける。
- [ ] 区間走行、停止、荷役、荷下ろしを別イベント化して位置を更新する。
- [ ] 明示された距離・速度・加減速・停止条件をシナリオに保持し、前提不足は計算不能とする。
- [ ] 同一入力での決定性、到達不能・移動中再割当などの回帰テスト。  
**受入:** 走行結果とイベント時計／パレット・AGF状態が一致。距離が未検証なら実測ETAを表示しない。

### M4 交通・干渉制御 — ☐
- [ ] 区間・交差点の占有／解放、反対方向の競合、待ちイベント。
- [ ] 1車線交互通行と2車線区間の利用条件を分離する。
- [ ] シャッター開閉・通行許可、進入前待機／通過イベントを扱う。
- [ ] 未確定のすれ違い・設備前通過は仮定で安全扱いせず、保留・設定不足として記録する。
- [ ] デッドロック／競合と通行順序の合成テスト。  
**受入:** 不許可区間の進入と禁止された同時占有が発生しない。

### M5 搬送・設備詳細化 — 🔄
- [ ] 01～05の各設備荷役点をM3走行完了と連動させる。
- [ ] 包装機・ラベル・回収、整列機・マガジン、仮置き、倉庫の容量・許可・占有を統合する。
- [ ] 同行02発行保留／ロケーション予約を維持し、取消・復旧時の予約解放を設計する。
- [ ] 未達設備・異常・通信異常時の保留／復旧操作の状態遷移と履歴。
- [ ] パレットIDを工程全体で保持する正常／異常テスト。  
**受入:** 走行・設備占有・在荷・搬送依存が一つのイベント列で整合する。

### M6 電池・充電 — 🔄
- [ ] 距離／動作別の消費を設定可能にし、仮定値を明示する。
- [ ] 充電場所までの物理移動、待ち行列、2口排他、終了・復帰をM3に統合する。
- [ ] 通常搬送の選定から除外する状態と、実行中タスクの扱いを検証する。
- [ ] 同時充電、復帰閾値、残量不足、空きなしの回帰テスト。  
**受入:** 搬送・充電が同じ時計と経路グラフで再現される。

### M7 再生・比較・分析 — 🔄
- [ ] イベント・区間状態からAGFをCAD上に表示（初期の模式AGF位置は使用しない）。
- [ ] 空走／積載／待ち／荷役／充電のAGFタイムライン。
- [ ] 要求・完了・未完了、設備滞留、区間待機、充電待ち、稼働率を集計。
- [ ] 同一の外生生産入力・初期条件で2方式を独立実行し、根拠と差分を出力。
- [ ] CSVにRun ID・入力条件・根拠区分・イベント／集計を残す。  
**受入:** UIとCSVが同じイベント履歴から再現できる。

### M8 統合受入 — ☐
- [ ] 3時間シナリオでの決定性と正常・異常・容量／同時刻境界テスト。
- [ ] 🔒 実図面／実経路の利用者レビューと未確定箇所一覧の確認。
- [ ] スマホ／PC操作、下書き保存・再読込、再生・比較・CSVの通し確認。
- [ ] 実CAD・派生形状・実座標・顧客／設備固有情報の公開漏えいがないことを確認。
- [ ] PRレビュー、CI確認、必要な承認後にmainへマージ。  
**受入:** 現場確認済み範囲と仮定値を明示したオフライン分析版として使用できる。

## 3. 次に着手する作業順

1. **M1:** 非公開参考ガイド4本のレビュー、荷役／停止／旋回／シャッターの点付け、寸法と原点の確認。
2. **M2:** 確認済み区間の走行可否・方向・車線・ゲートを型付きデータとして登録・検証。
3. **M3:** 合成経路で物理走行イベントと位置更新を実装してから、承認済み非公開マップと接続。
4. **M4～M6:** 交通予約、設備占有・保留、充電移動を順次統合。
5. **M7～M8:** 3時間の物理再生・比較・CSV・受入検証。

日程・期限は未合意のため設定しない。M1の非公開レビュー待ち中でも、M2～M4のデータ契約や合成テストは進められる。

## 4. 進捗更新ルール

- **作業単位のチェック:** [Issue #2](https://github.com/zony21/agf-simulator/issues/2) のチェックボックスを更新し、該当PR・コミット・CI・レビュー結果を添える。
- **工程の状態:** 全受入条件を満たしたときだけ ✅ にする。部分完成は 🔄 のままにする。未マージなら「PR実装済み」と表記する。
- **未確定は未確定:** 運用の確認済み仕様と、図上の点・距離・停止姿勢の承認状況は別に記録する。
- **機密保持:** 元DXF、現場固有のレイヤー・図面・実座標、私有の下書きJSONはこの公開リポジトリ／Issueに貼らない。
- **不一致解消:** この文書の固定スナップショットとIssueの最新チェックが異なる場合は、まずIssueの検証根拠を確認して文書を更新する。

## 2026-09-25 ローカルUI改訂（未公開）

ユーザー指示で既存ブランチをローカルmainへ早送り統合。今回のUI改訂は未コミット・未Push。5画面、802保管位置、倉庫南側の待機2／充電2の分離、時刻同期再生、イベント由来の稼働率・時間内訳、同一入力比較を追加。詳細は[UI実装範囲](ui-dashboard.md)。M7は合成イベントの分析を拡張した段階であり、CAD上の実走行・交通ボトルネックは未完了。M1〜M8の工程全体の完了チェックは進めない。
