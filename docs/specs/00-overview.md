# 00. AGF搬送シミュレーター 全体仕様書 v1.1

**文書種別：全体仕様書／対象：オフライン分析用シミュレーター**  
本書を親仕様とし、設備・通常搬送01～05・AGF・全体マップ・AGF通行の個別仕様書9冊を別文書として管理する。これは実設備の制御システムや安全機能の代替ではない。

## 1. 文書構成

| No. | 文書 | 対象 |
| --- | --- | --- |
| 00 | [全体仕様書](00-overview.md) | 目的、共通イベント、データ、画面、連携、受入条件 |
| 01 | [設備仕様書](01-equipment.md) | 設備台帳、容量、設備状態、配置、搬送許可 |
| 02 | [通常搬送01仕様書](02-transport-01.md) | パレタイザから包装機への製品搬送 |
| 03 | [通常搬送02仕様書](03-transport-02.md) | 包装機から指定倉庫位置への入庫 |
| 04 | [通常搬送03仕様書](04-transport-03.md) | 空パレット整列機からマガジンへの補充 |
| 05 | [通常搬送04仕様書](05-transport-04.md) | 仮置き場から包装機への再投入 |
| 06 | [通常搬送05仕様書](06-transport-05.md) | 仮置き場から指定倉庫位置への搬送 |
| 07 | [AGF仕様書](07-agf.md) | 車両状態、選定、残量、充電、走行、履歴 |
| 08 | [全体マップ仕様書](08-map.md) | 2D表示、設備・停止位置、ルート、シャッター、レイヤー、確定区分 |
| 09 | [AGF通行・車線仕様書](09-traffic.md) | 第1段階：通行可否、第2段階：方向・車線・同時すれ違い |

## 2. 目的と範囲

- 4台のAGF、製品パレット、空パレット、設備状態と搬送01～05を、同一時間軸の**離散イベント**で再現する。
- 3時間を初期再生範囲とし、イベントログと設備状態に基づく2Dリプレイを提供する。
- 「エリア優先あり／なし」の2方式を**同じ生産入力・初期状態**で比較する。
- 搬送要求、割当、空走、荷受け、実搬送、荷下ろし、包装・ラベル、補充、充電、倉庫入庫の順序関係を検証する。
- 実機PLC、WCS、RCSへの送信、実設備の安全判断、性能保証は対象外。
- **シミュレーターの設備配置・通路・シャッター・保管区画はCAD図のレイアウトに準拠**させる。現在の抽象論理マップ／模式図を実CADと一致した完成図とは扱わない。

## 3. 基準シミュレーション設定

| 項目 | 値 | 扱い |
| --- | --- | --- |
| AGF | 4台 | 確認済み |
| 充電器 | 2台 | 確認済み |
| 初期バッテリー | 各100% | シミュレーション初期値 |
| 1搬送消費 | 1～2% | 確認済み範囲 |
| 消費代表値 | 1.5% | **仮定・要確定** |
| 充電開始／通常搬送復帰 | 40%以下／80% | シミュレーション設定 |
| 充電速度 | 1%=2.4分 | シミュレーション設定 |
| パレタイザ | 8系列・各系列2PLバッファ | 確認済み |
| 01生成 | PLC搬出完了信号 | 確認済み |
| 7系列全稼働時の払出基準 | 過去最大実績10.2PL/hr | 参考実績、一定周期ではない |
| 包装機 | 1台、投入1PL・回収2PL | 確認済み |
| 包装タクト | 3.3分/PL（18PL/hr表記と丸め差あり） | 基準条件 |
| マガジン／整列機 | 各5台 | 確認済み |
| 03発生／補充 | 残数3枚への到達時／投入完了時+10PL | 確認済み |
| 仮置き場 | 3か所、04・05共通 | 確認済み |

実際のパレット搬出時刻と系列配分は入力イベント列で決定する。10.2PL/hrから一定間隔のPLC完了信号を自動的に「実績」として捏造しない。**シミュレーションに用いる設定値は設定画面から変更可能**にする。**8系列それぞれの搬出頻度（間隔）は分単位で個別設定可能**にし、設定値によって生成したイベントは実PLC実績と区別する。単位の小数桁、設定値の保存方式、変更中のRunへの反映タイミングは未確定。

## 4. システム構成と責務

| 機能 | 責務 |
| --- | --- |
| Scenario | 初期条件、設備容量、発生イベント、仮定値、選定方式、根拠区分 |
| Equipment | 各設備の状態・占有・許可・在荷と倉庫ロケーション |
| Task | 01～05の要求、優先順位、割当、実行、完了、保留、異常 |
| AGF | 4台の位置、モード、残量、作業・充電状態 |
| Event Engine | 条件判定、状態変更、後続イベント予約、履歴 |
| Layout | 設備・停止点・旋回点・走行グラフと確定／仮区分 |
| Replay UI | イベント由来の2D表示、再生、時刻シーク、履歴、比較 |
| Metrics | 要求／完了数、入庫数、各待ち時間、滞留、空走、稼働率 |

## 5. 共通のイベント処理規則

イベントは `timeMs`（非負整数）＋ `sequence`（同時刻内の安定順序）で一意に処理する。パレットID・タスクID・設備ID・AGF ID・位置IDを引き継ぎ、以下を独立したイベントとして残す。

1. 設備による製品搬出、空パレット消費、作業者の手動指示。
2. 搬送要求の生成、実行可能車両の選定と割当。
3. 空走、停止・荷役許可、荷受け、実搬送、荷下ろし、タスク完了。
4. 包装開始／完了、ラベル完了、出口到達／搬出OK、倉庫入庫。
5. 整列機搬送OK、補充要求、残数・投入許可、補充完了。
6. 充電要求、充電位置移動、充電待ち、充電開始／終了。

イベント前提が未成立なら進めず、保留・拒否理由を記録する。AGF1台の二重割当、パレット重複搬送、設備容量超過、許可のない荷役を許容しない。

## 6. 搬送間の依存関係

```text
製品搬出 ──→ 01 ──→ 包装・ラベル・出口OK ──→ 02 ──→ 倉庫入庫
               └──→ 例外時は仮置き ──→ 04 ──→ 包装・ラベル・出口OK ──→ 02
                                          └──→ 05 ──→ 倉庫入庫（受入条件確認）
空パレット消費 ──→ マガジン残数3枚 ──→ 03 ──→ 10PL補充実績
```

02は「必ず01のみを前工程とする」のではなく、04から再投入したパレットも、必要な包装・ラベル・出口OKを満たせば対象とする。05の未包装品の受入可否等は**未確定**であり、無条件に許可しない。

## 7. 画面と入出力

- 上部：再生／停止／速度／時刻スライダー／シナリオ／選定方式切替。
- **設定画面：** シミュレーションの設定値を変更できる。系列1～8ごとの搬出頻度を分単位で別々に設定し、Runの入力条件として保持する。
- **04・05の操作：** 画面のボタンから手動で対象パレット・仮置き場・搬送先を指定してタスクを予約する（対象の在荷・許可・二重予約を検証する）。
- 中央：簡易2D設備図、AGF1～4の色付き番号アイコン、設備占有、搬送・停止状態。
- 側部：バッファ数、整列機搬送OK、マガジン残数、充電器占有、AGF位置・タスク・残量。
- 下部：時刻付きイベント履歴、パレットIDの工程追跡、AGFタイムライン、CSV。
- **結果出力：** シミュレーション実行結果をCSVとして出力できる。Runと使用設定の対応、確定値／仮定値の区別を保持する。CSVの詳細な列構成・ファイル単位・文字コードは実装時に確定する。
- 比較：同じ外生入力で両方式を独立実行し、入庫、要求／完了、待ち、滞留、空走、充電待ち、稼働率を並列集計する。
- 実測／確認済み設定／仮定／未確定を画面・CSVで区別する。アニメーションの時計で工程の完了時刻を独自変更しない。

## 8. 配置・経路の取扱い

**シミュレーターの最終表示・経路はCAD図のレイアウトを順守する。** 設備・荷役・倉庫保管位置は外部レイアウトデータで表現する。表示・経路・停止点・確定区分の詳細は[全体マップ仕様書](08-map.md)と[経路指定ガイド](../route-annotation-guide.md)を参照する。実図・実座標はこの公開文書に含めない。走行線、個別シャッター停止点、系列ごとの荷受け停止点は未確定。交差点・曲がり角の旋回点は仮設定に限る。未確定エッジは仮表示とし、実距離・確定所要時間や安全判断に流用しない。

## 9. 受入条件

- 01はPLC搬出完了以前に生成しない。
- 02は対象PLの包装・ラベル・出口搬出OKが成立する前に生成しない。
- 03は残数3枚の到達時に一度だけ要求し、投入完了前に+10しない。
- 各設備の設定容量、車両1台1タスク、充電器占有上限を守る。
- 04・05は作業者の対象指定を記録し、仮置きの同一PLを二重搬送しない。
- 同じ入力・シードなら同じイベント履歴を生成する。シーク後も履歴と表示状態が一致する。
- 仮定データを実測・確定結果として表示しない。
- 設定画面からシミュレーション設定値を変更でき、系列ごとの搬出頻度を分単位で個別設定できる。
- 04・05は対象を指定してボタンからタスク予約できる。重複・許可不足の要求は無条件に発行しない。
- シミュレーション結果をCSV出力でき、どのRun・設定条件の結果か追跡できる。
- **AGFの通常選定は目的地（荷下ろし先）エリアにいる実行可能な車両を優先し、その中でバッテリー残量が少ない車両を優先する。** 詳細は[AGF仕様](07-agf.md)。

## 10. 実装状況・未確定

公開用の開発ブランチでは、目的地エリア優先選定・シナリオ駆動の離散イベント生成・01～05の合成モデル・充電・日本語画面・イベント再生・比較・CSVを実装した。系列別の初回搬出ずらし、初期AGF位置、倉庫ロケーション、工程時間などのデモ値は**合成・仮定値**であり、実PLC記録や設備実績ではない。実CAD準拠配置、実走行経路と所要時間、シャッター・車線占有・干渉制御、設備連携、実設備での性能保証は未実装である。詳細は [ハーネス契約](../harness-contract.md) を参照する。未確定事項は [決定待ち一覧](../open-decisions.md) で別管理する。
