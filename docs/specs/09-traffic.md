# 09. AGF通行・車線仕様書 v0.5（パレタイズエリア第2段階反映）

**独立仕様書**／関連：[全体マップ](08-map.md)・[設備](01-equipment.md)・[AGF](07-agf.md)・[経路指定ガイド](../route-annotation-guide.md)

> この文書は走行可否と車線の確認台帳。図面の矢印をそのまま「一方通行」「2車線」とは解釈しない。実図、実座標、現場識別情報、提供資料の名称はこの公開リポジトリに保存しない。

## 1. 確定の順序

1. **第1段階：通行できる場所を確定。** AGFが通過できる通路区間、設備への取付け通路、通行禁止区画、必要な通行許可を区間ごとに確定する。この段階では往来可能性、車線数、すれ違い可否、停止・旋回の安全性を確定しない。
2. **第2段階：第1段階で通行可能とされた区間ごとに、方向・車線数・すれ違いを確定。** 「双方向通行」と「2車線」を別属性とする。双方向1車線なら同時すれ違いの可否と退避／区間占有制御を別に確認する。
3. **第3段階（後続）：停止点・旋回点・優先順位・区間占有・距離・速度・時間を確認。** 前段階の未確定を推測で補完しない。

## 2. 判定単位と属性

**通路（Segment）**はシャッター、交差点、分岐、設備取付け口など、通行条件が変わる地点で分割する。設備・保管ロケーション自体と、そこへアクセスする走行通路を混同しない。

| 項目 | 値の例 | 確認段階 |
| --- | --- | --- |
| `segmentId` | 公開用の抽象ID | 初期 |
| `access` | `allowed` / `forbidden` / `unresolved` | 第1段階 |
| `accessScope` | 空走／積載／充電移動等、条件に差がある場合 | 第1段階 |
| `permission` | シャッター通行OKなど必要条件 | 第1段階 |
| `direction` | 双方向／指定方向のみ／未確定 | 第2段階 |
| `laneCount` | 1／2／その他／未確定 | 第2段階 |
| `simultaneousPassing` | 可／不可／制御条件付き／未確定 | 第2段階 |
| `turnStopStatus` | 確定／仮／未確定 | 第3段階 |
| `evidenceStatus` | 利用者確認／図示候補／未確定 | 全段階 |

通路方向、車線数、同時すれ違いは独立して記録する。矢印の本数は車線数を意味しない。製品パレットの保管マスは個別ロケーションであり、列間にAGFが進入する通路と区別する。利用者が「置き場ロケは通行可能」と補足したため、ロケーションへの進入・荷役は可能とするが、全マスを無条件に走行可能な道路としてグラフ化しない。

## 3. 第1段階：通行できる場所

### 3.1 既に確認されている運用制約

| 論理区分 | AGF通行 | 補足 |
| --- | --- | --- |
| 製品倉庫の通常出入口 | 東側出入口を使用 | 出入ルール確認済み。東側縦通路の車線数は第2段階で確認済み（出入口自体の車線数は別途確認） |
| 製品倉庫の西側出入口 | **使用しない** | AGFの経路候補から除外 |
| パレタイザエリアの入口 | 西側から進入 | 運用ルール確認済み |
| パレタイザエリアの出口 | 東側から退出 | 運用ルール確認済み |
| 空パレット保管マス | AGFの03直接荷受け対象外 | 整列機と保管場所を区別 |
| 個別製品保管マス | 個別ロケーションを搬送先として扱う | マス内部を走行路として自動登録しない |

上表は全走行線の確定を意味しない。シャッターの開閉・通行可能信号の前提も、単なる走行線と区別する。

### 3.2 製品倉庫の通行可否台帳（第1段階の回答反映）

以下の通行可否は利用者の回答で確定。**通行可能とは「その区間をAGFの走行候補として扱える」ことだけであり、双方向・車線数・同時すれ違い・旋回可否を含まない。** 各IDは区間群を識別するための抽象IDであり、厳密な分割・座標を確定しない。

| 区間ID | 通行区間 | 第1段階：通行可否 | 第2段階：方向／車線／すれ違い |
| --- | --- | --- | --- |
| WH-X-L | 下段の防火シャッター経由の東西接続通路 | **通行可能（確認済み）** | §5の確定値を参照 |
| WH-E-V | 東側縦方向通路 | **全区間通行可能（確認済み）** | §5の確定値を参照 |
| WH-W-V | 西側縦方向通路 | **全区間通行可能（確認済み）** | §5の確定値を参照 |
| WH-W-A | 西側・上段保管区画への横方向アクセス | **通行可能（確認済み）** | §5の確定値を参照 |
| WH-W-B | 西側・中央保管区画への横方向アクセス | **通行可能（確認済み）** | §5の確定値を参照 |
| WH-W-C | 西側・下段保管区画への横方向アクセス | **通行可能（確認済み）** | §5の確定値を参照 |
| WH-E-A | 東側・上段保管区画へのアクセス | **通行可能（確認済み）** | §5の確定値を参照 |
| WH-E-B | 東側・下段保管区画へのアクセス | **通行可能（確認済み）** | §5の確定値を参照 |
| WH-X-U | 上段の防火シャッター経由の東西接続通路 | **代表入庫経路として通行可（確認済み）** | 双方向・2車線・同時すれ違い可能（第2段階で回答） |
| WH-ROW | 製品パレットの横一列（1行）の通路 | **AGFが進入可能（確認済み）** | 行単位：双方向・1車線。行単位の対向車の同時すれ違い可否は未確定 |
| WH-SERVICE | 整列機・HP／充電位置への接続通路 | **取付け経路未確定** | 未確定 |

西側上・中央・下／東側上・下は、この確認で使った区間群のラベルであり、実際の道路数やすべての分岐点を表すものではない。**ロケーションを示す四角は製品パレットの置場**。マスを走行路とみなさず、列間の通路と保管マスを別に登録する。

### 3.3 個別の倉庫保管順序

**「奥から順にパレットを設置する想定」**を保管方針の検討条件として記録する。走行可否や車線数を示す情報ではない。奥の基準方向、列ごとの格納順序、格納可否・予約、各ロケーションの停止位置は今後別途確定する。既存の個別ロケーション管理は維持する。

### 3.4 既に確定した利用制約／今も未確定の事項

- 倉庫東側出入口を使用し、西側出入口はAGFが使用しない。
- シャッターの通行許可確認は経路の通行可否と別に必要。
- 今回の回答は**製品倉庫内の通行可否**を更新したもので、パレタイザ側の個別通路、各シャッター停止座標、旋回可能位置を確定しない。
- 製品倉庫の9区間の走行方向・車線数・同時すれ違いは第2段階の回答を反映する。横1行の通路は双方向・1車線。ブロック単位の双方向往来と区別し、行単位の対向車の同時すれ違い可否、詳細な行・停止位置、減速条件等は未確定。

## 4. 第1段階の確定方法

対象となる区間の可否を利用者回答で確定する。第1段階で通行可能な区間を、通行方向・車線数・同時すれ違いの確認対象に移す。設備取付けや個別の列間通路で範囲が未確定の部分は、画像上で区間分割するまで未確定のまま保持する。

## 5. 第2段階：方向・車線・往来条件（回答反映）

利用者の第2段階回答を以下に記録する。**第1段階で確定した通行可否は維持する。** 車線数は論理区間の値であり、車幅・すれ違い余裕・停止姿勢の安全性を証明するものではない。

| 区間ID | 進行方向 | 車線数 | 対向AGFの同時すれ違い | 確定状況 |
| --- | --- | --- | --- | --- |
| WH-X-L：下段防火SHの東西接続 | 双方向 | 2車線 | 可能 | **確認済み** |
| WH-E-V：東側縦通路 | 双方向 | 2車線 | 可能 | **確認済み** |
| WH-W-V：西側縦通路 | 双方向 | 2車線 | 可能 | **確認済み** |
| WH-W-A：西側上段アクセス | 双方向 | 1車線 | 不可、交互通行 | **確認済み** |
| WH-W-B：西側中央アクセス | 双方向 | 1車線 | 不可、交互通行 | **確認済み** |
| WH-W-C：西側下段アクセス | 双方向 | 1車線 | 不可、交互通行 | **確認済み** |
| WH-E-A：東側上段アクセス | 双方向 | 1車線 | 不可、交互通行 | **確認済み** |
| WH-E-B：東側下段アクセス | 双方向 | 1車線 | 不可、交互通行 | **確認済み** |
| WH-X-U：上段防火SHの東西接続 | 双方向 | 2車線 | 可能 | **確認済み** |
| WH-ROW：製品パレット横1行の通路 | 双方向 | 1車線 | 未確定（行単位） | **方向・車線は利用者確認済み** |

### 5.1 製品倉庫の「行」と「ブロック」の区別（Q11・Q12確定）

- **横一列（1行）単位：双方向・1車線。** AGFはその行を相互に通行できる。1車線であることから対向する2台の同時すれ違いを可能とは推測しない。
- **ブロック単位：双方向の往来が可能。** 複数の行で往来できるという管理単位の説明であり、ブロック全体が物理的に2車線という意味には置き換えない。
- 利用者が述べた「製品倉庫置き場まで2車線」は**横1行を単位に見た説明**。以前の「2車線アプローチ区間から1車線進入区間へ変化する実際の境界を特定する」という解釈を破棄する。道路の物理的な車線減少地点は新たに設定しない。
- WH-X-U、WH-X-L、WH-E-V、WH-W-Vなど、個別に2車線であると確認された幹線・接続通路の設定と、各アクセス区間の1車線設定はそのまま維持する。
- 図示された保管マスは個別の製品パレット置場。行内通路・ロケーション・停止点を別の管理対象とし、実際の行数・接続・格納方向は後続で確認する。

### 5.2 同一行の02要求・ロケーション予約（Q13・Q14確定）

1. 02の前提である対象PLの包装・ラベル貼付・出口搬出OKが成立した時点で、対象ロケーションと所属行を確認する。
2. **同じ行に既存の置きタスクがある場合、既存タスクの完了まで新しい02タスクを発行しない。** 発行待ち理由と対象PLを保持し、完了後に再判定する。
3. 同行の置きタスクがない場合、ロケーションの空き・予約状況・受入可否を確認する。利用可能なら02発行時に当該ロケーションを予約し、02タスクを発行する。
4. 発行待ちの要求はAGFに割り当てない。予約ロケーションは別のタスクに重複割当しない。荷下ろし完了時の在庫更新は搬送仕様に従う。

上記は**02発行制御**として確認済み。通過するだけのAGFまで同一行から排除する、02発行時に行全体の物理占有を開始する、05にも同一の発行ルールを適用する、といった拡張は未確定。置きタスクの占有開始・解除の厳密なイベント時点や取消時の予約解放は後続で決定する。

### 5.3 同一行・隣接行の干渉

- 同じ行に置きタスクがあれば、少なくともQ14により新規02の発行を保留する。
- **隣接する別の行に2台が同時進入することは可能**だが、近接時には減速する。減速率・距離閾値・開始／解除条件は未確定。
- 既に1車線・交互通行と確認した区間は対向車の同時すれ違いを不可とする。横1行内の同時すれ違いは未回答として残し、隣接行の同時進入と混同しない。

## 6. パレタイズエリア：第1段階の通行可能範囲（P1～P4確定）

図上の「走行通路1」「走行通路2」は**別々の横方向通路**。これらの名称は車線数ではない。図面の上／下、左／右は表示上の方向であり、未指定の寸法・曲がり角・停止位置を補完しない。

| 区間ID | 確認対象 | 第1段階：通行・接続 | 第2段階 |
| --- | --- | --- | --- |
| PZ-A1 | 走行通路1（設備側・上段） | **図示された全区間でAGF通行可能** | §6.3の回答値を参照 |
| PZ-A2 | 走行通路2（下段） | **図示された全区間でAGF通行可能** | §6.3の回答値を参照 |
| PZ-CON-W | 走行通路1⇄2の西端接続 | **相互に移動できる接続あり** | §6.3の回答値を参照 |
| PZ-CON-E | 走行通路1⇄2の東端接続 | **相互に移動できる接続あり** | §6.3の回答値を参照 |
| PZ-DEV | 各PGW・PM・包装機への接続 | **いずれも走行通路1から接続** | 方向・車線数・すれ違いは§6.3の回答値を参照。個別停止位置は未確定 |
| PZ-OT | OT1～OT3への接続 | **いずれも走行通路1から接続** | 方向・車線数・すれ違いは§6.3の回答値を参照。個別停止位置は未確定 |
| PZ-EXT | エリア間接続通路 | 図示と既存のエリア出入ルールを保持 | 全区間の個別通行・方向・車線数・すれ違いは未確定 |

既存の「パレタイズ西側から進入／東側から退出」「製品倉庫は東側から出入／西側SHはAGFが使用しない」を維持する。走行通路1と2は、**西端と東端の両方でつながる**。設備別に別の接続通路を追加したり、通路2から設備へ直接接続できると推測したりしない。

### 6.1 AGF同士の干渉制御

- **干渉制御はAGF側で実施する**ことを確認済みとする。
- **タスク実行中のAGF同士は、お互いが通行できるように制御する**。タスク中の対向車を恒久的な通行不可として扱わず、経路・交通制御で通過可能な状態を目指す。
- 第2段階ではP5～P10のすべてに「同時すれ違い可能」と回答されている。ただし**各区間は1車線**との回答でもあるため、同時通過する具体的な位置関係・必要空間・制御方法は未確定。無停止通過、確定した安全性、各区間がそれぞれ2車線であることは意味しない。
- 1台が荷役中の設備前で他車が通れるか、エリア間通路および各シャッターでどう排他するかも未確定。

### 6.2 通路1・2を組み合わせた2車線の取扱い（P5・P6補足）

利用者の補足は「走行通路1,2は双方向二車線の扱いにすればいい」。このため**通路1と通路2を一つの通路群として見たときに双方向2車線扱い**とする。P5・P6で各通路に個別に回答された「双方向・1車線」は維持する。通路1自体、通路2自体をそれぞれ2車線に増やさない。

「対向する2台が別々の通路1・2を使ってすれ違う」運用と、「通路1の同一区間内で2台が横に並んですれ違う」運用は異なる。今回の回答では各区間の同時すれ違いが「可能」とされているが、**どちらの通過形態を指すか、個々の1車線接続部での通過方法は未確定**。実装で同一1車線の2台同時占有や安全なすれ違いを無条件に成立させない。

### 6.3 第2段階の回答台帳（P5～P10）

| 区間ID | 進行方向 | 区間単位の車線数 | 同時すれ違い（回答値） | 管理上の扱い |
| --- | --- | --- | --- | --- |
| PZ-A1：走行通路1 | 双方向 | 1車線 | 可能 | 通路1・2を合わせて双方向2車線扱い |
| PZ-A2：走行通路2 | 双方向 | 1車線 | 可能 | 通路1・2を合わせて双方向2車線扱い |
| PZ-CON-W：西端接続 | 双方向（通路1⇄通路2） | 1車線 | 可能 | 具体的な同時通過形態は未確定 |
| PZ-CON-E：東端接続 | 双方向（通路1⇄通路2） | 1車線 | 可能 | 具体的な同時通過形態は未確定 |
| PZ-DEV：PGW・PM・包装機の接続 | 双方向（通路1⇄設備） | 1車線 | 可能 | 各設備の荷役停止点・通過方法は未確定 |
| PZ-OT：OT1～OT3の接続 | 双方向（通路1⇄仮置き場） | 1車線 | 可能 | 各OTの荷役停止点・通過方法は未確定 |

P5～P10の「可能」を**利用者の回答値として記録**する。これと別に、シミュレーションで同一区間へ2台同時進入させるには、使用通路・通過形態・占有／退避制御が確定する必要がある。

### 6.4 継続する未確定事項

- 「同時すれ違い可能」は、通路1・2を別々に使う対向通行を指すのか、1車線と回答された区間**内部での対向2台の同時通過も含む**のか。
- 西端／東端の接続、設備・仮置き場の取付け部分での譲り合い、停止・待機、交差点占有。
- 干渉制御の開始・解除、停止位置、速度・減速率、区間別距離、シャッターでの詳細条件。

未確定事項は確定した「通行可能」「双方向」「各区間1車線」「通路1・2の組合せ2車線」という回答値を覆さない。曖昧な部分だけを追加確認する。

## 7. 受入条件・更新規則

- 第1段階で`forbidden`の区間は経路探索対象にしない。
- `unresolved`は通行可能扱いにせず、確認待ちを表示する。
- 片方向を確定するまでは、図示の矢印だけで逆方向の可否を推測しない。
- 製品倉庫で双方向2車線・同時すれ違い可能と確認済みの区間はその条件を使用する。1車線・交互通行と確認済みの区間は対向車の同時すれ違いを許さない。横1行内の同時すれ違いは未確定とする。
- パレタイズエリアのPZ-A1/A2/CON-W/CON-E/DEV/OTは、第1段階の通行可否・接続と第2段階の方向・車線数・すれ違い回答値を維持する。通路1・2は各1車線、2本の組合せで双方向2車線扱い。1車線区間内部での2台同時通過は、その具体的な通過形態・占有制御を確認するまで自動的に成立させない。
- 02は同じ行の既存置きタスクが完了する前に発行しない。発行時に空き・予約を確認し、利用可能なロケーションを予約する。
- 実ルート、停止点、旋回位置、衝突回避が未確定の段階では実機相当の安全性や確定ETAを主張しない。
- 変更ごとに改訂番号と対象区間を記録し、ユーザー確認済み事項を繰り返し未確定扱いに戻さない。
