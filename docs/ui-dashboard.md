# ローカルダッシュボードの実装範囲

2026-09-25のデザイン指示と更新済み倉庫仕様を反映。ローカル `main` 上の確認用変更で、今回のコミット・Push・PR作成・公開は行わない。

| 画面 | 実装内容 |
| --- | --- |
| 搬送モニター | SVG概念マップのズーム・パン・全体／倉庫表示、AGF選択、搬送元／先の強調、4台の状態・電池・タスク・積載・所属エリア、2口の充電器、設備在荷、イベント絞り込み |
| 製品倉庫詳細 | WB1〜3・EB1〜2の行列段、2段切替、802保管位置、東第10列除外、サンプル在庫・予約・使用不可、位置選択 |
| シミュレーション設定 | 基本、8系列の間隔と初回ずらし、4台の初期エリア／電池、電池・充電、設備容量・工程時間。単位と未反映状態を明示 |
| 搬送タスク管理 | 表示時刻の01〜05、区分／完了状態の絞り込み、04・05予約ダイアログ、03のマガジン使用・整列機準備入力 |
| 結果分析 | 全期間の要求・完了・保留・未完了、待機／充電／充電待ち／荷下ろし待ち時間、要求待ち、AGF稼働率・タイムライン・搬送別実績 |
| 比較分析 | 同じシナリオ・外生入力から選定方式だけ変更した2回の結果、横並び集計・差分 |
| CAD・経路レビュー | 既存の端末内SVG/PNG読込、図上下書き編集、Undo/Redo、別台帳での点・共有接続・区間確認、JSON保存／再読込を維持 |

再生は `requestAnimationFrame` で表示時刻だけを1・2・4倍で進め、最後の `timeMs <= 表示時刻` のイベントの保存スナップショットを描画する。同時刻の全イベント適用後を使い、未来の結果を途中時刻へ混入しない。先頭への停止、シーク、次イベントへの移動でもシミュレーションを再計算しない。

設定変更は明示的な実行まで保存済み結果へ反映しない。実行失敗・予約エラーでも前の結果を保持する。手動操作は表示時刻に入力を追加した候補シナリオを計算し、成功した場合だけ置き換える。二重予約、場所不一致、未許可、入庫先を確保できない場合は拒否理由を表示する。未反映設定がある間は、先に実行するよう案内する。

分析は各スナップショットの継続時間を実行終了まで積分する。作業稼働率は `(空走・荷受け + 積載・荷下ろし) / 実行期間`。充電・充電移動・荷下ろし待ちは別計上。待機時間はAGFの未割当時間、要求待ちはタスク発行から割当（未割当なら実行終了）までの合計。発行前の02保留はタスク数と分ける。現エンジンは走行と荷役を一部同じ状態で扱うため、分析もまとめて表示する。

CSVはUTF-8 BOM付き。保存済み結果の全イベント、実行識別子、方式、元／先、状態、根拠区分、先頭行にシナリオJSONを含む。未反映の編集設定を出力結果へ混ぜない。

電池の初期設定は走行・荷役360分で70ポイント消費。供給元の想定を利用者が共有し、対象時間はユーザー確認済み。待機・荷下ろし待ち・充電待ちを除外し、充電場所への移動を含める。画面のイベント間の残量は保存状態と経過時間から共通の純粋関数で求める。旧タスク単位方式も明示選択でき、CSVには方式・根拠・時間範囲と設定値を保持する。稼働率では充電移動を別集計するが、電池消費時間には含める。詳細は[AGF仕様](specs/07-agf.md)。

構造・設備用途は[倉庫の概念契約](warehouse-layout-contract.md)の確認値。在庫、保管位置ID、初期AGFエリア、搬出のずらし、移動・荷役時間等は明示した合成／仮定値。標準・充電競合・手動予約の3つの再現可能なデモを用意する。充電競合デモは初期41%、系列1〜4が60分間隔、系列5〜8停止、初回ずらし0分。容量上限を守りながら2口の充電待ちを発生させるための合成入力である。

実走行位置・姿勢、選択AGFの物理経路ハイライト、距離別消費、実シャッター信号、交通占有・衝突回避、実機のボトルネック分析は未実装。概念通路は破線で表示し、承認済み実経路は未登録と明示する。CAD上へ仮のAGF位置を重ねず、M3〜M8の完了を主張しない。

検証：`npm run check`。追加回帰は802位置・空列除外・待機／充電枠の分離・禁止区画、同時刻シーク、倍率・終端、状態継続時間、同一入力比較、予約エラー時の不変性を対象とする。PC／狭い画面の操作確認と画像はローカルの非公開レビュー領域に保存する。
